import TeamsPadApp from '@/components/answer-pad/TeamsPadApp';

/** The Teams meeting side panel: one address for teachers and students. */
export default function AnswerPadTeamsPage() {
  return <TeamsPadApp />;
}
