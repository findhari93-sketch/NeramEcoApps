import BrowserPadApp from '@/components/answer-pad/BrowserPadApp';

/** The Answer Pad in any browser: enter the room code from the teacher's console. */
export default function AnswerPadPage() {
  return <BrowserPadApp />;
}
