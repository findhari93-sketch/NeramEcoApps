/**
 * Temporary stub. Task 13 replaces this with the real Add exemplar sheet
 * (teacher-only: upload a reference image and tag it for Inspiration).
 */
export default function AddExemplarSheet(_: { open: boolean; onClose: () => void; onCreated: (id: string) => void }) {
  return null;
}
