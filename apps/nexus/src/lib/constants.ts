/**
 * Default admin Teams contacts — used as fallback if the setting API fails.
 * The authoritative list is stored in nexus_settings table (key: 'admin_teams_contacts')
 * and is configurable from the admin settings page.
 */
export const DEFAULT_ADMIN_TEAMS_EMAILS = [
  'TamilSelvan@neramclasses.com',
  'Haribabu@neramclasses.com',
];
