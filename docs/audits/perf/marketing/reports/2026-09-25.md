# Marketing performance audit, 2026-09-25

This session fixed findings from the 2026-09-24 audit (`2026-09-24.md`): the security issues that could be fixed in code alone, and the week 1 items in `agents/seo-aeo/nata-coaching-ranking-strategy.md`. All changes are uncommitted and nothing is deployed.

## Scope and coverage
- Planned: fixes only; no new units audited. Three findings were added while verifying (PERF-0045, PERF-0046, PERF-0047).
- Coverage after this session: 101 of 402 units have an audit record. The units changed today will read `stale:code` next session, and that is expected.
- Evidence sources used:
  - Vitest (96 marketing tests plus the student app's `phone-match` test, all passing).
  - `tsc --noEmit` on `apps/marketing` and `apps/app` (exit 0).
  - `next dev` on port 3010, checked with curl.
  - Playwright screenshots at 375px and 1280px.
  - curl against production for the new measured findings.

## Performance audit report
1. **Top problems:** unchanged from 2026-09-24, minus the ones fixed below. Still open, worst first:
   - PERF-0002 (storage, needs a migration)
   - PERF-0005 (no Razorpay webhook)
   - PERF-0014 (city cannibalisation, needs a decision)
   - PERF-0015 (hard-coded ratings)
   - PERF-0017 (global bundle)
   - PERF-0019 (colleges dynamic)
2. **Production errors reported by the user:** none were reported. Every page logged about 20 duplicate-key errors from the header menu; that is fixed (PERF-0047).
3. **Failing endpoints named by the user:** none named.
4. **Why refresh fixes it:** does not apply.
5. **Slowest APIs:** not measured this session.
6. **Duplicate requests:** the five public content APIs can now be served from Vercel's edge cache (PERF-0016, partial). Edge hit rate not measured.
7. **Largest components and bundles:** not measured.
8. **Database and query bottlenecks:** no change.
9. **Most expensive renders:** no change.
10. **Recommended fixes:** see open findings.
11. **Estimated impact:** see BEFORE / AFTER.
12. **Risk**
    - **Payment verify now rejects a row id that does not belong to the paid order.** Both payment flows send the id returned by create-order for that same order, so real payments are unaffected.
    - **Order creation now refuses an expired or invalid coupon with a 400**, instead of charging the full fee silently.
    - **verify-phone now requires that the Firebase token carries the same phone number.** LoginModal already force-refreshes the token after linking the phone, so the claim is present.
13. **Implementation order for what is left**
    1. PERF-0009, 0024, 0025, 0026 (security, code only).
    2. PERF-0015 (ratings).
    3. PERF-0005 (webhook).
    4. PERF-0017, 0019, 0020, 0021.
    5. PERF-0014 and PERF-0031 (content and URL decisions).
14. **Needs approval before doing**
    - PERF-0002: storage migration.
    - PERF-0005: webhook secret.
    - PERF-0014: which city URL system survives.
    - PERF-0036: unique index on applications.
    - PERF-0039: function region.
    - PERF-0046: real faculty photos.
15. **Fixed this session** (all uncommitted)

| Finding | Fix | Test or check |
|---|---|---|
| PERF-0001 | Verify binds the update to the signed order and to pending rows; constant-time signature compare; replay returns the receipt without rerunning side effects; enrolment write errors are logged | `lib/payments/razorpay-verify.test.ts` |
| PERF-0007 | Coupons validated on the server (expiry, usage limit, per-lead, max cap); the client's discount figure is ignored | `lib/payments/coupon-discount.test.ts` |
| PERF-0003 | Unsigned token fallback removed; the enrolling user's uid is no longer returned | tsc |
| PERF-0006 | Phone marked verified only when it matches the token's `phone_number`, in marketing and in the student app | `lib/phone.test.ts`, `apps/app/src/lib/phone-match.test.ts` |
| PERF-0008 | Refund status scoped to the signed-in user, staff-only columns dropped, `no-store` | tsc |
| PERF-0037 | Refund button sends its ID token | tsc |
| PERF-0022 | capture-phone reads insert errors instead of calling `.catch()` on a builder | tsc |
| PERF-0004 | State route moved to a whole-segment `[stateSlug]`, same public URL; unknown states 404 | `lib/seo/state-coaching-slug.test.ts`; localhost shows the Tamil Nadu page correctly and Goa returns 404 |
| PERF-0010 | `/_next/` no longer disallowed | localhost robots.txt |
| PERF-0011 | Catch-all redirect excludes `best-nata-coaching-chennai` | path-to-regexp check; localhost 200 |
| PERF-0012 | Sitemap courses come from `data/courses.ts`; noindexed locale college URLs removed; testimonials and achievements added | localhost sitemap: 719 URLs, 0 `/ta/colleges` |
| PERF-0013 | `public/logo.png` (512) and `public/og-default.png` (1200x630) rendered from the brand SVGs; the layout uses the PNG | localhost 200 |
| PERF-0029 | Brand suffix removed from 62 titles in 40 files; the home page uses `title.absolute` | localhost titles |
| PERF-0030 | `buildAlternates` emits hreflang only for indexable locales and sends noindexed copies to the English canonical | `lib/seo/indexable-locales.test.ts` (also guards drift from `next.config.js`) |
| PERF-0045 | Layout canonical removed; nata-hub, jee-barch-hub, ask-seniors and colleges/compare get their own | localhost canonical |
| PERF-0018 | Hero h1 and subtitle visible at first paint on home and /nata-2026; global `prefers-reduced-motion` rule | screenshots |
| PERF-0027 | SponsoredBanner rotates after mount | tsc |
| PERF-0028 | `[locale]/not-found.tsx` plus a catch-all, so 404s keep the header and footer; root not-found has html and body | screenshot at 375px. Dev serves 404s through its error shell, so confirm server rendering on staging |
| PERF-0047 | Header menu keys made unique | 0 duplicate-key errors |
| PERF-0016 (partial) | `Vercel-CDN-Cache-Control` on the 5 public content routes; a test fails if an authenticated route uses it | `app/api/_lib/public-cache.test.ts` |

## Skipped (fresh: audited and unchanged)
- Not applicable. This was a fix session.

## BEFORE / AFTER

| | BEFORE | AFTER |
|---|---|---|
| Page load | Hero heading faded in from opacity 0 | Hero heading painted at first paint (LCP not measured) |
| API calls per screen | 3 marketing-content function calls per page view | Same 3 requests, now cacheable at the edge for 5 minutes (hit rate not measured) |
| Slow APIs | Not measured | Not measured |
| React errors | About 20 duplicate-key errors per page | 0 |
| Hydration errors | 1 inferred on /colleges | 0 inferred |
| Major UX problems / improvements | 22 broken state pages, 404s from internal links, 4 pages canonicalised to home, 404 page without navigation | State pages correct, links fixed, own canonicals, 404 inside the site layout |
| Sitemap | 1,783 URLs, including 40 dead course URLs and noindexed locale college pages | 719 URLs locally, none dead or noindexed. The local database has fewer colleges than production, so the production count will be higher |
